export const HOME = "/";
export const RECENTS = `${HOME}recents`;
export const RECOVERY = `${HOME}recovery`;
export const STORAGE = `${HOME}storage`;

// Files
export const FILES = `${HOME}files`;
export const FILE = `${FILES}/:fileId`;

// Fallbacks
export const NOT_FOUND = "*";
